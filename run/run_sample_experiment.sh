#!/usr/bin/env bash
# Simple experiment: start iperf server on h4 and client on h1 for 10s
# Usage: run this inside mininet CLI or by remote exec. Example: sudo mn --custom ... --controller=remote
echo "Run inside Mininet CLI: 'h4 iperf3 -s -D' and then 'h1 iperf3 -c 10.0.0.4 -t 10'"
#!/usr/bin/env bash
# placeholder: paste full code here
